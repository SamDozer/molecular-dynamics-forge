# Alpha-zein (A8HNE1) MD analysis pipeline -- Python dependencies
# Tested with Python 3.11.  Versions listed are those the pipeline was validated
# against; the lower bounds are generally safe.

numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
pyyaml>=6.0            # mdforge config-driven workflows

# MD trajectory analysis
MDAnalysis>=2.7        # validated with 2.10.0
mdtraj>=1.10           # validated with 1.11.1 ; provides built-in DSSP
panedr>=0.8            # read GROMACS .edr directly (no `gmx energy` needed)

# Machine learning / clustering / networks
scikit-learn>=1.3      # validated with 1.9.0
networkx>=3.0          # residue interaction network

# Interaction fingerprints
prolif>=2.0            # validated with 2.2.0
rdkit>=2023.3          # ProLIF backend (validated with 2026.3.3)
