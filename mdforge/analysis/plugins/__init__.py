"""
Plugin package.

Drop a ``*.py`` file here (or point ``--plugin-dir`` at an external folder) that
defines a :class:`~mdforge.core.base.BaseAnalysis` subclass, and it is
auto-discovered and exposed through the CLI without touching the core.
See ``example_end_to_end.py`` for a minimal template.
"""
